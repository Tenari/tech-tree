<%= form_for(@generic_tool) do |f| %>
  <% if @generic_tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@generic_tool.errors.count, "error") %> prohibited this generic_tool from being saved:</h2>

      <ul>
      <% @generic_tool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
